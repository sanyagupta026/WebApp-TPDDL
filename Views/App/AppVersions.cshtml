@model IEnumerable<WebApp.Models.AppVersion>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>AppVersions</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AppVersion1)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AppVersion1)
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.AppId }) |
                @Html.ActionLink("Details", "Details", new { id=item.AppId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.AppId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
